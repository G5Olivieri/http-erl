-record(http_uri, {
          scheme = <<"http">> :: bitstring(),
          username = <<>> :: bitstring(),
          host = <<>> :: bitstring(),
          port = <<"80">> :: bitstring(),
          path = <<"/">> :: bitstring(),
          query = <<>> :: bitstring()
         }).

